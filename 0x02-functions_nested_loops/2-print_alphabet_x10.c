#include "main.h"
/**
 * main - function that prints 10 times the alphabet, in lowercase
 * Return: (0) (Success)
 */

void print_alphabet_x10(void)
{
	int x, y;

	for (x = 1; x <= 10; x++)
	{
		for (y = 97; y <= 122; y++)
		{
			_putchar(j);
		}
		_putchar('\n');
	}
	return (0);
}
