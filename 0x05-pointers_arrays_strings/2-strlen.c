#include "main.h"
/**
 * _strlen -  function that returns the length of a string.
 * @s: to store string length
 * Return: length of string
 */

int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
	length++;
	return (length);
}
